# Logo Óptica Anthony
Coloca aquí el archivo oficial del logo (ej. `logo.png` o `logo.svg`) proporcionado por el cliente.
Mientras tanto, la aplicación utiliza el lockup tipográfico vectorizado de alta fidelidad `BrandLogo.tsx`.
